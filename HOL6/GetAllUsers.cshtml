@model System.Collections.Generic.IEnumerable<HOL6.Models.User>

<table width="50%">
    @foreach (var user in Model)
    {
        <tr>
            <td align="center">@user.UserName</td>
            <td align="center">@user.password</td>
        </tr>
    }
</table>

